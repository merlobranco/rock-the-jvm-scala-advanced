package lectures.part2fp

object MoarFunctionalCollections {

  /*
    Remember Seq and Map are partial functions
    With certain index they return an element or throw an error
      - if the index is bigger than the size - 1 of the Seq
      - if the map does not contain such as provided key
   */

}
